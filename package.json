{
  "name": "buildgeneratorsxs",
  "private": true,
  "version": "7.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/app.js && node --check js/module-loader.js && node --check js/workers/optimizer-worker.js && node --check sw.js && npm test"
  }
}
